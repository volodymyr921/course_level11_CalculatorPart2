package com.shpp.p2p.cs.vomelianchuk.assignment11.functions;

/**
 * An interface that contains a method for calculating functions.
 */
public interface IAction {
    /**
     * Calculates the value of a function for a given number.
     *
     * @param number the number for which the function is calculated
     * @return function calculation result
     */
    double calculateFunction(double number);
}
